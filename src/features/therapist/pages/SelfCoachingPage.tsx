import { SelfCoaching } from "../components/SelfCoaching";

export const SelfCoachingPage = () => {
  return (
    <div className="min-h-screen bg-background">
      <SelfCoaching />
    </div>
  );
};
