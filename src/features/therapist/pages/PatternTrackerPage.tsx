import { PatternTracker } from "../components/PatternTracker";

export const PatternTrackerPage = () => {
  return (
    <div className="min-h-screen bg-background">
      <PatternTracker />
    </div>
  );
};
